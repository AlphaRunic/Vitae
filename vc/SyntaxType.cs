namespace vc {
    public enum SyntaxType {
        Number,
        Whitespace,
        Plus,
        Minus,
        Multiply,
        Divide,
        Modulo,
        OpenParen,
        ClosedParen,
        Invalid,
        EOF
    }
}
